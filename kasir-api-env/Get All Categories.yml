info:
  name: Get All Categories
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
