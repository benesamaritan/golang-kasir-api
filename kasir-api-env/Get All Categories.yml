info:
  name: Get All Categories
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8000/categories
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
