info:
  name: Category Add
  type: http
  seq: 10

http:
  method: POST
  url: "{{url}}/categories"
  body:
    type: json
    data: |-
      {
        "name": "",
        "description": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
