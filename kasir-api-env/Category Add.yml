info:
  name: Category Add
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8000/categories
  body:
    type: json
    data: |-
      {
        "nama": "Oleh-oleh",
        "deskripsi": "Halal"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
