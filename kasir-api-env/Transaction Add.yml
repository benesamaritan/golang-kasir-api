info:
  name: Transaction Add
  type: http
  seq: 11

http:
  method: POST
  url: "{{url}}/api/checkout"
  body:
    type: json
    data: |-
      {
      "items": [
        {
          "product_id": 9,
          "quantity": 5 
        }
      ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
