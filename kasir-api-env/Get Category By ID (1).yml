info:
  name: Get Category By ID (1)
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/categories/3"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
