info:
  name: Report hari ini
  type: http
  seq: 13

http:
  method: GET
  url: "{{url}}/api/report/hari-ini"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
