info:
  name: Report hari ini
  type: http
  seq: 12

http:
  method: GET
  url: "{{url}}/api/report/today"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
