info:
  name: API Listing
  type: http
  seq: 14

http:
  method: GET
  url: "{{url}}/api"
  headers:
    - name: X-API-Key
      value: secret
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
