info:
  name: Get All Products
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/produk"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
