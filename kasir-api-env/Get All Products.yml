info:
  name: Get All Products
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/product"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
