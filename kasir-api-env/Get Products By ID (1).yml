info:
  name: Get Products By ID (1)
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/product/1"
  headers:
    - name: X-API-Key
      value: secret
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
