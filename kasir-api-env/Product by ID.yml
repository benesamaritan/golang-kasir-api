info:
  name: Product by ID
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{url}}/product/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
